/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'thebangladeshtoday.com',
            'www.dailypost.net',
        ]
    }
}

module.exports = nextConfig
